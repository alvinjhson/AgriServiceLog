{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ESNext",                                 /* Use the latest ECMAScript features. */
    "lib": ["DOM", "DOM.Iterable", "ESNext"],           /* Include DOM and ES features for React. */
    "jsx": "react-jsx",                                 /* Use the new JSX transform (React 17+). */

    /* Modules */
    "module": "ESNext",                                 /* Use ES module syntax. */
    "moduleResolution": "node",                         /* Resolve modules using Node.js-style. */
    "baseUrl": "./",                                    /* Specify the base directory for imports. */
    "paths": {                                          /* Define path aliases for cleaner imports. */
      "@/*": ["src/*"]
    },

    /* Interop Constraints */
    "esModuleInterop": true,                            /* Enable compatibility with CommonJS modules. */
    "allowSyntheticDefaultImports": true,               /* Allow default imports from modules with no default export. */

    /* Type Checking */
    "strict": true,                                     /* Enable strict type checking options. */
    "forceConsistentCasingInFileNames": true,           /* Ensure consistent casing in imports. */

    /* Emit */
    "skipLibCheck": true                                /* Skip type checking for libraries to speed up builds. */
  },
  "files": ["src/global.d.ts"],                         /* ADD THIS LINE */
  "include": ["src"],                                   /* Include all files in the `src` folder. */
  "exclude": ["node_modules"]                           /* Exclude `node_modules` from compilation. */
}

